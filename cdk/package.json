{
  "name": "ratel-docs-cdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "synth": "npm run build && cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.157.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
